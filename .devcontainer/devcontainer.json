{
  "name": "SynapseML Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "conda init && conda activate synapseml && sbt setup",
  "containerEnv": {
    "JAVA_HOME": "/usr/lib/jvm/java-8-openjdk-amd64"
  },
  "extensions": [
    // List your preferred VS Code extensions here
  ]
}
